"use client";

import React from "react";

type Props = {};

const Library = (props: Props) => {
  return (
    <section
      id="Library"
      className="bg-BGCOLOR-SECONDARY rounded-lg flex flex-col p-5 gap-y-4 font-semibold h-full overflow-y-auto"
    >
      Library
    </section>
  );
};

export default Library;
